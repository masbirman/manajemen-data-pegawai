# Database Configuration
POSTGRES_USER=user
POSTGRES_PASSWORD=change_this_secure_password
POSTGRES_DB=pegawai_db

# Backend Configuration
DATABASE_URL=postgresql://user:change_this_secure_password@db:5432/pegawai_db
SECRET_KEY=change_this_to_a_very_long_random_secret_key_minimum_32_characters

# Frontend Configuration (untuk production)
REACT_APP_API_URL=https://your-domain.com/api

# Production Notes:
# 1. Copy file ini menjadi .env
# 2. Ganti semua password dan secret key
# 3. Update REACT_APP_API_URL dengan domain production Anda (gunakan /api untuk reverse proxy)
# 4. Jangan commit file .env ke git (sudah ada di .gitignore)
# 5. Setup nginx reverse proxy di server untuk route /api ke backend dan /uploads ke backend
